all:
  vars:
    ansible_connection: local
  children:
    etcd:
      hosts:
        localhost:
    kube_control_plane:
      hosts:
        localhost:
    k8s_cluster:
      hosts:
        localhost:
