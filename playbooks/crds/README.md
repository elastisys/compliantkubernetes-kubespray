# CRDs

Currently used to define the ServiceMonitors CRD early.

The CRD should be kept in sync with the one in the latest Apps release.

Be sure to set `CK8S_GITHUB_TOKEN` and allow it to access the Apps repository.

```sh
./sync.sh
```
